https://github.com/Gyurmatag/adaptive-llm-gateway-aws

STATUS: PUBLIC. Final URL. Generate the QR from exactly this string.

Scrub verified clean before publishing: no account IDs, no real ARNs, no
secrets, no .env tracked. The only arn:aws: strings are a foundation-model ARN
template (empty account field) and an AWS-managed IAM policy ARN. Both
handoff screenshots are local-dashboard captures with no account data in
frame.

Re-run the scrub before the talk, since the deployment work still to come is
exactly what introduces account identifiers:

  grep -rEn '\b[0-9]{12}\b|arn:aws:[a-z0-9-]+:[a-z0-9-]*:[0-9]{12}:' \
    --exclude-dir=.git --exclude-dir=node_modules --exclude-dir=.next \
    --exclude-dir=upstream --exclude-dir=.venv .

Owner: Gyurmatag (confirmed with the speaker, matches the @gyurmatag handle on
slide 1). Licence: MIT (LICENSE committed).

If it ever needs to go back to private:
  gh repo edit Gyurmatag/adaptive-llm-gateway-aws --visibility private --accept-visibility-change-consequences
